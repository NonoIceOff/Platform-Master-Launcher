# Token GitHub (permission "repo") — utilisé par npm run release
GH_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxx

# URL de pm-api (comptes + versions). En dev local : http://localhost:3000
PM_API_BASE=http://localhost:3000
